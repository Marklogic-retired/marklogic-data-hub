'use strict';
// declareUpdate(); // Note: uncomment if changing the database state

var collectionName;

// TODO:  produce the  output from the input variables
fn.count(fn.collection(collectionName))
