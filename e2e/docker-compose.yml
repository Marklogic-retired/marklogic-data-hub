version: '3.7'
services:
  cypress:
    build: 
      context: .
      dockerfile: Dockerfile
    image: "e2e"
    environment:
      - CYPRESS_baseUrl=http://${HOSTNAME}:80
    working_dir: /e2e
    volumes:
      - ./:/e2e